# Public API

`openapi.yaml` is the canonical Adenosine REST contract. Generated Go server code and the public TypeScript client belong under `api/generated/` and `packages/api-client/`.

Run `make generate` after changing the contract. Do not hand-edit generated files.
